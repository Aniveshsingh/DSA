# include <iostream>
using namespace std;

int main() {
   
    // // array creation
    // int brr[101];
    // char ch[12];
    // bool flag[100];
    // long live[1000];
    // short ar[100];

    // cout<<"Array created successfully!"<<endl;

    // int arr[3] = {1, 2, 3};     // array initialization

    // cout << "Base address of arr: " << arr << endl;
    // cout << "Base address of arr: " << &arr << endl;

    // cout << "what it will point to address or elements ?: " << arr[1] << endl;
    // cout << "what it will point to address or elements ?: " << &arr[1] << endl;

    // cout<< " size of arr: " << sizeof(arr) << endl;

    // int crr[5] = {5, 8, 9, 12, 13};
    
    // int n = 5;
    // for (int i = 0; i < n; i++) {
    //     cout << "Element at index " << i << ": " << crr[i] << endl;
    // }

    // problem statement
    // 1. 10 size array banao
    // 2. 10 elements input lo
    // 3. double-up each value of the array

    return 0;
}